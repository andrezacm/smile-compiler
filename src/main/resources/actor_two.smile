agent ActorTwo
	covers ( role ActorTwo, agent ActorThree )
	isPartOf ( agent ActorFour )

ExternalRelationships
	dependsOn ( role ActorFour ) for ( goal SomeGoal )
	dependsOn ( actor ActorOne ) for ( task SomeTask )
	reaches ( goal SomeGoal ) to ( actor ActorOne )
	reaches ( goal SomeGoal ) to ( actor ActorFour )