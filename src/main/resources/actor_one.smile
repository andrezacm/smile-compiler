actor ActorOne
	covers ( role ActorTwo, agent ActorThree )
	isPartOf ( actor ActorFour )
	isA ( actor ActorFive, position ActorSix )

ExternalRelationships
	dependsOn ( role ActorTwo ) for ( goal SomeGoal )
	dependsOn ( actor ActorFour ) for ( task SomeTask )
	reaches ( goal SomeGoal ) to ( role ActorFour )
	carriesOut ( task SomeTask ) to ( actor ActorFive )